name: karsu
description: "KARSU — Build. Grow. Hit. AI-powered entrepreneurship platform."
publish_to: "none"
version: 0.3.4

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.0
  http: ^1.2.0
  shared_preferences: ^2.5.3
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
